spring:
  application:
    name: void-users
  datasource:
    url: jdbc:postgresql://void-db-users:5432/void-db
    username: u
    password: p
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
